class Solution
{
public:
    int countCompleteSubstrings(string word, int k)
    {
        // k,k+k,k+k+k
        int cnt = 0;
        int n = word.size();
        for (int i = k; i <= n; i++)
        {
                }
    }
};